Javascript 10/30 CheckBoxes

What I learned:

Use inBetween as variable to get current and last checked box
then use Array.prototype.filter() to get items in that range

event.shiftKey can be a useful property